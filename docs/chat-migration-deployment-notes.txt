Chat Migration Deployment Notes
Date: 2026-03-06
Scope: Forwarding fields + remove-for-you support

Migrations involved
1. `2026_03_06_152310_add_forwarding_columns_to_messages_table`
2. `2026_03_06_152509_add_hidden_at_to_message_receipts_table`

Deployment order
1. Deploy backend code that includes both migration files.
2. Run migrations:
   - `php artisan migrate --force`
3. Deploy API/controller/model updates that start writing these fields.
4. Deploy frontend features that read forward/remove-for-you state.

Why this order
- Schema-first deployment avoids runtime SQL errors when new code writes new columns.
- Frontend is deployed last to avoid requesting data fields that backend cannot yet provide.

Rollback order
1. Roll back latest two migrations together (if needed):
   - `php artisan migrate:rollback --step=2`
2. Re-deploy backend code without new field usage.
3. Keep frontend features disabled/hidden until schema is re-applied.

Verification checklist executed locally
- `php artisan migrate` succeeded.
- Nullability check:
  - `messages.forwarded_from_message_id` => nullable
  - `messages.forwarded_from_user_id` => nullable
  - `messages.forwarded_snapshot` => nullable
  - `message_receipts.hidden_at` => nullable
- FK check:
  - `messages.forwarded_from_message_id` -> `messages.id` with `ON DELETE SET NULL`
  - `messages.forwarded_from_user_id` -> `users.id` with `ON DELETE SET NULL`
- Index check:
  - `message_receipts_user_hidden_at_idx` on (`user_id`, `hidden_at`)
- Rollback path check:
  - `php artisan migrate:rollback --step=2` succeeded
  - schema objects were removed
  - `php artisan migrate` re-applied both migrations successfully

Current status
- Migration execution and rollback safety: Verified
